fastapi>=0.115
uvicorn>=0.30
httpx>=0.27

# 上游 TLS 验证跟系统证书库走：卡巴斯基这类杀软的 HTTPS 扫描是 MITM，
# httpx 自带的 CA 捆绑包不认它的根证书，没有这个就是 502 self-signed certificate
truststore>=0.9

# Windows 托盘。Linux/Mac 用 --no-tray 跑，装上了也用不到
pystray>=0.19
pillow>=10.0

# 开发 / 测试
pytest>=8.0

# 可选：只有某个站的「出口」要填 socks5:// 时才用得上（保存出口时会检查并提示）
# socksio>=1.0
