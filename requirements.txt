fastapi>=0.115
uvicorn>=0.30
httpx>=0.27

# Windows 托盘。Linux/Mac 用 --no-tray 跑，装上了也用不到
pystray>=0.19
pillow>=10.0

# 开发 / 测试
pytest>=8.0

# 可选：只有某个站的「出口」要填 socks5:// 时才用得上（保存出口时会检查并提示）
# socksio>=1.0
